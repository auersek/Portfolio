// #pragma once

// #include <string>
// #include <ostream>

// #include "ast_node.hpp"        // for Node
// #include "ast_type_specifier.hpp"   // for EntitySpecifier

// namespace ast {

// class Context; // forward-declared, we only use it as a reference

// class TypeSpecifierClass : public Node {
// public:
//     virtual ~TypeSpecifierClass() = default;

//     virtual void EmitRISC(std::ostream& stream, Context& context, std::string& destReg) const = 0;
//     virtual std::string getId() const = 0;
//     virtual EntitySpecifier getEntity() const = 0;
//     virtual void Print(std::ostream& stream) const = 0;
//     virtual TypeSpecifier getTypeSpecifierEnum() const = 0;git st
// };

// }
